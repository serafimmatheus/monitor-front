services:
  frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:5555}
    container_name: monitor-front
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
